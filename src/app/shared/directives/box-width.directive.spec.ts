import { BoxWidthDirective } from './box-width.directive';

describe('BoxWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new BoxWidthDirective();
    expect(directive).toBeTruthy();
  });
});
